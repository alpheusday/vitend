/**
 * Index module
 * @module index
 */

export type {
    ErrorHandler,
    ServerHandler,
    ServerMiddleware,
    ServerOptions,
    ServerPlugin,
    ServerRequest,
} from "srvx";

export { defineServer } from "#/functions/define";
